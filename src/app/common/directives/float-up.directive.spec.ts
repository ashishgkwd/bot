import { FloatUpDirective } from './float-up.directive';

describe('FloatUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatUpDirective();
    expect(directive).toBeTruthy();
  });
});
