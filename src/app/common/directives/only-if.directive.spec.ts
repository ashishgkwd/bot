import { OnlyIfDirective } from './only-if.directive';

describe('OnlyIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyIfDirective();
    expect(directive).toBeTruthy();
  });
});
